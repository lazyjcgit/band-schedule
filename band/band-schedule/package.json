{
  "name": "band-schedule",
  "version": "1.0.0",
  "description": "手环课表 - 小米手环 Vela 快应用（显示今天要上的课和下一节课）",
  "engines": {
    "node": ">=8.10"
  },
  "scripts": {
    "start": "aiot server --watch --open-nuttx",
    "build": "aiot build",
    "release": "aiot release",
    "watch": "aiot watch --open-nuttx",
    "lint": "eslint --format codeframe --fix --ext .ux,.js src/"
  },
  "devDependencies": {
    "@aiot-toolkit/velasim": "^0.1.26-beta.3",
    "@aiot-toolkit/jsc": "^1.0.3",
    "aiot-toolkit": "1.1.0",
    "babel-eslint": "^10.0.1",
    "eslint": "^6.8.0",
    "eslint-plugin-ux": "^0.0.4",
    "ux-types": "0.0.9"
  },
  "browserslist": [
    "chrome 65"
  ],
  "allowScripts": {
    "sharp@0.32.6": true
  }
}
